{
  "description": "API to query the MLB Stats API and return with the outcome of today's Phillies game.",
  "name": "phillies-win-api",
  "author": "Chris Leidich",
  "scripts": {
    "install": "yarn install --force --ignore-scripts --modules-folder pode_modules",
    "test": "invoke-pester ./tests/*.ps1",
    "build": "psake",
    "start": "./server.ps1"
  },
  "version": "1.0.0",
  "main": "./server.ps1",
  "license": "WTFPL 2"
}
